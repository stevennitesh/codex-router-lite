# Sol execution handoff: thread admission

Authority: [delivery plan revision 1](switchyard-thread-admission-plan.md).
Assignment `THREAD-ADMISSION`, native GPT-5.6 Sol Medium, one reusable implementer.
Lead owns consequential choices, review and acceptance; Sol owns implementation,
investigation and validation. Do not delegate implementation.

Checkout: `E:\GitHub\code\codex-router`, main. Whole-plan baseline
`3f352ae7f08759cc03c4c80b3a636d9ffe93f28e`. Pre-dispatch changes are these two
planning files and a component-guide pointer only. Preserve them and any other
work; stop on unexpected competing edits. Read AGENTS, engineering contract and
conditional debugging/Switchyard/certification owners as needed.

Worker-only method: verify then read
`C:\Users\steve\.agents\skills\ponytail-implementer\SKILL.md`.
SHA-256: `adc416210ef7f062ef0b302a519267d3b188fa01155b44d475f9789677d5ccff`.
Missing/mismatched identity is a prerequisite return; do not substitute a method.

Initial custody covers plan steps 1–4 only: small hardening candidate, tests and
review evidence. No commit, push, deploy or promotion. Plan step 5 is real desktop
evidence; do not substitute synthetic parser tests for that acceptance. Maintain
v1/draft source and unchanged installed production until the release transaction.

Previous real-path discovery: standalone app-server `turn/start.toolOutput`
serializes final items with `id,name,namespace,output,type`; optional item turn
metadata was absent. The fco UUID differs from current turn ID. The request has
JSON x-codex-turn-metadata. Do not revive rejected mandatory item-metadata guards.
Prove actual receiver thread field before requiring it. The fresh synthetic app
task is titled “Switchyard desktop switching test”; use its synthetic conversation
only if needed, never unrelated tasks. Messages through app tools differ from
typed user messages. Native collaboration and app-task lifecycle are separate tests.

Return ready/blocked, baseline and changed scope, decisive tests and source
references, real-path coverage/limits, unresolved decisions, and confirmation all
writers/processes stopped and custody explicitly released. Questions retain
custody unless explicitly released. Lead uses change-review and sends repairs to
this same Sol; no parent implementation takeover. Use 180-second event waits while
delegated; no routine progress narration. No finished-child working state.

## Steps 1-4 candidate

Ready for lead review against baseline `3f352ae7f08759cc03c4c80b3a636d9ffe93f28e`.
The installed `codex-cli 0.155.0-alpha.9.2` tag resolves to upstream commit
`4607249e430dac1c961df4dc615beae88e33cec8`. Its app-server constructs
`turn/start.toolOutput` with null call identity and no item metadata, while its
Responses metadata owns the current receiver `thread_id` and `turn_id`. A fresh
isolated installed-binary probe confirmed those fields on the actual wire: the
receiver matched the created synthetic task, the final item had a native `fco_`
identity and no `call_id`, and item metadata was absent.

The Router candidate excludes every non-null `call_id`, preserves and compares
source/receiver UUID identity, treats proven self-delivery as non-delegation, and
uses the existing null projection for malformed or unavailable required identity.
Focused routing tests, the full 254-test `npm run verify`, source checks and the
installed Codex catalog compatibility check pass. The retained non-Sol assertion
uses a stateful simulated Switchyard in the existing local Router integration
fixture; it proves the Router response boundary, not the real Switchyard binary.
The locked Rust affinity implementation and evidence are unchanged. No runtime was replaced.
Real Windows desktop delivery, continuation/replay correlation and exact-route v2
recertification remain explicit promotion gates.
